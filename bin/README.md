# Prebuilt Binaries

This repository tracks editable scripts/config and module packaging files.

Prebuilt runtime binaries are **not** uploaded through the current connector flow:
- `bin/uperf`
- `bin/busybox/busybox`

Use the packaged release zip to get these binaries, or copy them from upstream module base package.
